/**
 * Generate playId when user preview the animation effect of a single widget
 * @param option
 * @returns
 */
export declare function usePreviewPlayId(option: {
    isPreview: boolean;
    previewId: number;
}): number;
