/// <reference types="react" />
/**
 * The sub-components of this component will use theme.
 * Used to create an area using `theme` in a `theme2` area.
 */
export declare const ThemeArea: ({ children }: {
    children: any;
}) => JSX.Element;
